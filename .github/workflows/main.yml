name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check runner
        run: echo Runner is working on %RUNNER_OS%

      - name: Show PAYARA_HOME
        run: echo PAYARA_HOME = %PAYARA_HOME%
